<div class="container">
  <div class="row justify-content-center">
    <div class="col-8">
      <h1>Add tags to plant</h1>

      <% # the form wild build this route 👉🏻 POST '/plants/:plant_id/plant_tags' %>
      <%= simple_form_for [ @plant, @plant_tag ] do |f| %>
        <%= f.input :tag, collection: Tag.all, input_html: { multiple: true, class: "multiple-select" } %>
        <%= f.submit "Add tag", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
